<UserControl x:Class="OwTracker.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="16" MaxWidth="520" HorizontalAlignment="Left">
        <TextBlock Text="Settings" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16" />

        <TextBlock Text="{Binding AutoAcceptThreshold, StringFormat='Auto-accept threshold: {0:P0}'}" />
        <Slider Minimum="0.5" Maximum="1.0" Value="{Binding AutoAcceptThreshold}"
                TickFrequency="0.01" IsSnapToTickEnabled="True" Margin="0,4,0,16" />

        <TextBlock Text="{Binding ReviewThreshold, StringFormat='Review threshold: {0:P0}'}" />
        <Slider Minimum="0.3" Maximum="0.97" Value="{Binding ReviewThreshold}"
                TickFrequency="0.01" IsSnapToTickEnabled="True" Margin="0,4,0,16" />

        <TextBlock Text="Retrain batch size" />
        <TextBox Text="{Binding RetrainBatchSize}" Width="100" HorizontalAlignment="Left" Margin="0,4,0,16" />

        <Button Content="Open Training Data Folder" Padding="12,6" HorizontalAlignment="Left"
                Command="{Binding OpenTrainingDataFolderCommand}" />
    </StackPanel>
</UserControl>
