<UserControl x:Class="OwTracker.App.Views.HeroReviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Hero Review Queue" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
            <TextBlock Text="{Binding UnreviewedCount, StringFormat=({0} pending)}"
                       Margin="8,0,0,0" VerticalAlignment="Center" Opacity="0.6" />
            <Button Content="Refresh" Margin="16,0,0,0" Padding="12,4"
                    Command="{Binding RefreshCommand}" />
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding PendingLabels}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Predicted" Width="140"
                                    DisplayMemberBinding="{Binding PredictedHero}" />
                    <GridViewColumn Header="Confidence" Width="100"
                                    DisplayMemberBinding="{Binding Confidence, StringFormat=P0}" />
                    <GridViewColumn Header="Captured" Width="160"
                                    DisplayMemberBinding="{Binding CapturedAt, StringFormat=g}" />
                    <GridViewColumn Header="Confirm As" Width="180">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox Width="160"
                                          ItemsSource="{Binding DataContext.HeroNames,
                                                        RelativeSource={RelativeSource AncestorType=UserControl}}"
                                          SelectedItem="{Binding ConfirmedHero, Mode=TwoWay}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="" Width="90">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Confirm"
                                        Command="{Binding DataContext.ConfirmCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
