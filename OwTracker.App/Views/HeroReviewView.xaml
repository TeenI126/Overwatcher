<UserControl x:Class="OwTracker.App.Views.HeroReviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900"
             Background="{StaticResource BgBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,0,1" Padding="22,18,22,16">
            <DockPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="HERO REVIEW" Style="{StaticResource H1}" />
                    <TextBlock Text="{Binding UnreviewedCount, StringFormat=({0} pending)}" Style="{StaticResource Crumb}" VerticalAlignment="Bottom" Margin="12,0,0,3" />
                </StackPanel>
                <Button DockPanel.Dock="Right" HorizontalAlignment="Right" Content="Refresh" Command="{Binding RefreshCommand}" />
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Margin="22,16,22,16" Style="{StaticResource Inset}">
            <ListView ItemsSource="{Binding PendingLabels}" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextBrush}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Predicted" Width="140" DisplayMemberBinding="{Binding PredictedHero}" />
                        <GridViewColumn Header="Confidence" Width="100" DisplayMemberBinding="{Binding Confidence, StringFormat=P0}" />
                        <GridViewColumn Header="Captured" Width="170" DisplayMemberBinding="{Binding CapturedAt, StringFormat=g}" />
                        <GridViewColumn Header="Confirm As" Width="180">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox Width="160"
                                              ItemsSource="{Binding DataContext.HeroNames, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              SelectedItem="{Binding ConfirmedHero, Mode=TwoWay}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Confirm"
                                            Command="{Binding DataContext.ConfirmCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</UserControl>
