<UserControl x:Class="OwTracker.App.Views.SessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Sessions" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
            <Button Content="Refresh" Margin="16,0,0,0" Padding="12,4"
                    Command="{Binding RefreshCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Sessions}" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserSortColumns="True" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Start" Width="*"
                                    Binding="{Binding SessionStart, StringFormat=g}" />
                <DataGridTextColumn Header="End" Width="*"
                                    Binding="{Binding SessionEnd, StringFormat=g}" />
                <DataGridTextColumn Header="Active" Width="120"
                                    Binding="{Binding ActiveDuration, StringFormat=hh\\:mm\\:ss}" />
                <DataGridTextColumn Header="Total Open" Width="120"
                                    Binding="{Binding TotalOpenDuration, StringFormat=hh\\:mm\\:ss}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
