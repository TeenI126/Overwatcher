<UserControl x:Class="OwTracker.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- Defined here (not in parent Window) so StaticResource lookup works in a UserControl -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Live session timer -->
        <Border Grid.Row="0" Padding="16" Background="#F2F4F8" CornerRadius="8">
            <StackPanel>
                <TextBlock Text="Current Session" FontSize="14" FontWeight="SemiBold" Opacity="0.7" />
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <StackPanel Margin="0,0,40,0">
                        <TextBlock Text="Active" FontSize="12" Opacity="0.6" />
                        <TextBlock Text="{Binding Watcher.CurrentActiveDuration, StringFormat=hh\\:mm\\:ss}"
                                   FontSize="28" FontWeight="Bold" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Total Open" FontSize="12" Opacity="0.6" />
                        <TextBlock Text="{Binding Watcher.CurrentTotalDuration, StringFormat=hh\\:mm\\:ss}"
                                   FontSize="28" FontWeight="Bold" />
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Quick stats + scrape control -->
        <Grid Grid.Row="1" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,40,0">
                    <TextBlock Text="Matches Scraped" FontSize="12" Opacity="0.6" />
                    <TextBlock Text="{Binding MatchCount}" FontSize="22" FontWeight="SemiBold" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Total Active Play" FontSize="12" Opacity="0.6" />
                    <TextBlock Text="{Binding TotalActiveTime, StringFormat=hh\\:mm\\:ss}"
                               FontSize="22" FontWeight="SemiBold" />
                </StackPanel>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <!-- CommunityToolkit.Mvvm strips "Async" suffix: StartScrapeAsync → StartScrapeCommand -->
                <CheckBox Content="Ignore duplicates" VerticalAlignment="Center" Margin="0,0,16,0"
                          IsChecked="{Binding IgnoreDuplicates}"
                          ToolTip="Walk the whole list instead of stopping after 3 consecutive already-scraped matches (back-fill)." />
                <TextBlock Text="Scraping…" Margin="0,0,12,0" VerticalAlignment="Center" Opacity="0.6"
                           Visibility="{Binding IsScraping, Converter={StaticResource BoolToVisibility}}" />
                <Button Content="Delete History" Padding="16,10" Margin="0,0,10,0"
                        Foreground="#B00020"
                        Command="{Binding DeleteHistoryCommand}" />
                <Button Content="Scrape Last Match" Padding="16,10" Margin="0,0,10,0"
                        Command="{Binding ScrapeLastMatchCommand}" />
                <Button Content="Scrape Last 10" Padding="16,10" Margin="0,0,10,0"
                        Command="{Binding ScrapeRecentCommand}" />
                <Button Content="Start Scrape" Padding="20,10"
                        Command="{Binding StartScrapeCommand}" />
            </StackPanel>
        </Grid>

        <!-- Scrape log (auto-scrolls to latest entry) -->
        <Border Grid.Row="2" Margin="0,16,0,0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6">
            <ListBox x:Name="ScrapeLogList"
                     ItemsSource="{Binding ScrapeLog}" BorderThickness="0"
                     FontFamily="Consolas" FontSize="12"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto" />
        </Border>
    </Grid>
</UserControl>
